# リンクリスト Webアプリ 操作説明

このドキュメントは、リンクリスト Webアプリを使う人向けの操作説明です。

## 起動方法

1. `index.html` をブラウザで開きます。
2. Web Storage に登録済みのリンク情報が読み込まれ、リンク一覧が表示されます。
3. 画面下部の右側に `originalId` が表示されます。

初回起動時など、Web Storage にリンク情報がない場合は、`JSON取込` から `links_XX.json` を取り込んでください。

## 画面構成

- 上部: 検索欄、検索ボタン、リセットボタン、JSON取込ボタン
- AreaB: 登録済みリンクJSONの表示切替、登録情報の更新、ログ表示、アイコン検索
- AreaC: カテゴリ一覧
- AreaD: リンク一覧、ログ、登録情報更新画面
- 下部: 日付情報、`originalId`

## JSONを取り込む

1. 上部の `JSON取込` ボタンをクリックします。
2. 取り込みたい JSON ファイルを選択します。
3. `links_01.json` から `links_99.json` 相当のリンク情報を取り込めます。
4. `function_enhancement.json` も取り込めます。

すでに同じIDのリンク情報が Web Storage に登録されている場合は、取込方法を選択します。

- `上書き`: 既存データをインポートしたデータで置き換えます。
- `マージ`: 既存データとインポートデータを結合します。

マージ時に `Name` と `URL` が両方一致するリンクは重複データとして扱います。重複時の扱いは次から選択します。

- 登録されているデータのみ残す
- インポートするデータで上書きする
- 登録されているデータの `note` に、インポートするデータの `note` を追記する

## JSONを出力する

AreaB の登録情報一覧または管理画面から `JSON出力` をクリックすると、Web Storage に保存されているJSONをファイルとして出力できます。

## 表示するリンク情報を切り替える

AreaB には、Web Storage に登録されているリンクJSONの一覧が表示されます。

- 常時表示: アプリ全体で表示対象にするかを切り替えます。
- AreaB初期表示: 起動時にAreaBで一時表示するかを切り替えます。
- AreaBのチェックボックス: 現在の画面で一時的に表示対象を切り替えます。

## 検索する

1. 上部の検索欄に検索キーを入力します。
2. `検索` ボタンをクリックします。
3. 一致したリンクだけが AreaD に表示されます。

複数の検索キーをスペース区切りで入力すると、AND検索になります。

## カテゴリで絞り込む

AreaC のカテゴリチェックボックスをクリックすると、そのカテゴリに一致するリンクを絞り込めます。

AreaC の区切り見出しは、かなの頭文字でまとめられます。清音・濁点・半濁点は同じグループとして扱われます。

## アイコンで検索する

AreaB のアイコンをクリックすると、そのアイコンに設定された検索キーが検索欄に追加され、リンク一覧が絞り込まれます。

アイコンと検索キーの対応は `function_enhancement.json` の `key_icon` で設定します。

## リセットする

上部の `リセット` ボタンをクリックすると、ページを再読み込みします。

## 登録情報を更新する

1. AreaB の `登録情報の更新` ボタンをクリックします。
2. AreaD に管理画面が表示されます。
3. 上部の `links_XX` ボタンから編集したいリンクJSONを選びます。
4. フォームの内容を編集します。
5. `Web Storage更新` ボタンをクリックします。

管理画面では、Web Storage に登録されているリンクJSONの削除やJSON出力もできます。

## function_enhancement.jsonを編集する

管理画面の Web Storage 一覧に `function_enhancement.json` が表示されている場合、`修正` ボタンから編集できます。

主な設定項目:

- `originalId`: このWebアプリの識別子です。画面右下やログCSVファイル名、ログの `originalId` 列で使用されます。
- `operationLogMax`: Web Storage に保持する操作ログの最大件数です。
- `key_highlight`: AreaC のカテゴリ強調表示色です。
- `key_icon`: AreaB のアイコン検索に使うアイコンと検索キーです。
- `targetDateList`: 日付イベント表示の設定です。
- `targetWeekList`: 曜日イベント表示の設定です。

## 操作ログを見る

AreaB の `ログ表示` ボタンをクリックすると、AreaD に操作ログが表示されます。

ログには次の情報が記録されます。

- 日時(UTC)
- 日時(ロケール)
- 曜日
- originalId
- 操作
- 詳細

ログに記録される主な操作:

- アプリ起動
- JSON取込
- JSON出力
- AreaB のチェックボックス変更
- AreaC のチェックボックス変更
- リンククリック
- 検索
- リセット

検索、AreaB/AreaCのチェックボックス変更、起動、リセットでは、表示されたリンク件数も `displayedLinkCount` として記録されます。

## 操作ログをCSV出力する

ログ表示画面で `CSV出力` をクリックすると、全ログをCSV形式で出力できます。

CSVファイル名は次の形式です。

```text
{originalId}__{最初のログ日時}__{最後のログ日時}__{行数}.csv
```

日時にはロケール時刻が使われます。行数は7桁のゼロ埋めです。

## データ保存場所

このWebアプリのデータは、ブラウザの Web Storage に保存されます。

別のブラウザや別の端末では同じデータは共有されません。必要に応じてJSON出力とJSON取込を使って移行してください。
